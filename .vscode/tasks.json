{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "cargo debug build dist-sys",
            "type": "shell",
            "command": "cargo", // note: full path to the cargo
            "args": [
                "build",
                "-p",
                "dist-sys",
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
